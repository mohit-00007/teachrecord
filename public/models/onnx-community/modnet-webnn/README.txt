TeachRecord Pro Matte model assets are staged by:
  npm run prepare:pro-matte

The build downloads the Apache-2.0 licensed ONNX Community MODNet assets locally and packages them into the application. The installed app is configured for local-only model loading.
